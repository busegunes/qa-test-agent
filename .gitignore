# Read-only mirrors of the source project repositories.
# Regenerated by `npm run sync` - never committed, never modified by hand.
.cache/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Node
node_modules/
npm-debug.log*
