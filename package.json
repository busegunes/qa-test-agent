{
  "name": "qa-test-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "QA knowledge base and test case generation workspace for the RMS, OMS and WMS projects.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "sync": "node scripts/sync-repos.mjs",
    "index": "node scripts/build-index.mjs",
    "check": "node scripts/build-index.mjs --check",
    "export": "node scripts/export-csv.mjs",
    "log:run": "node scripts/log-run.mjs",
    "refresh": "npm run sync && npm run index && npm run export"
  }
}
